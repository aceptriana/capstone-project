html
	head
		title= 'Studensite | Universitas Kuningan'
		link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous")
		script(src='https://code.jquery.com/jquery-3.3.1.min.js')
		script(src="/socket.io/socket.io.js")
		link(rel="icon" href="https://studentsite.uniku.ac.id/img/logo.png")
		style(type="text/css").
			.jumbotron {
				padding: 4rem 2rem;
				margin-bottom: 2rem;
				background-color: #33414e;
				border-radius: .3rem;
			}

			

		script.
				var socket = io();
				socket.on('connect', function() {
					console.log('browser client connected')
				});

				socket.on('success', function(res) {
					window.location.href=res.auth;
				});

			function authorizeApplication(event){
				window.location.href='/authorize'
			}

	body.d-flex.flex-column.h-100
		.jumbotron
			.container
				img(src='https://studentsite.uniku.ac.id/img/header_img.png')
				
		.main.flex-shrink-0.mb-5
			block content

		footer.mt-auto.py-3.bg-dark.text-white.text-center
			.container
				.row
					.col
						p.mb-0 Copyright © 2023 - Kuningan | Security Verify Universitas Kuningan © All Rights Reserved | Powered by Acep Triana

